import { Metadata } from "next"


export const metadata: Metadata = {
    title: "Login - Juno!",
    description: "Log in now to start using  Juno!"
}

export default function page(){
    return (
        <p>login page</p>
    )
}